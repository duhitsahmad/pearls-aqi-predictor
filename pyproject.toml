[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "pearls-aqi-predictor"
version = "0.1.0"
description = "Pearls AQI Predictor"
requires-python = ">=3.12,<3.13"

[tool.setuptools.packages.find]
where = ["src"]